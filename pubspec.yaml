name: takepack
description: A minimal Flutter startup app for TakePack.
publish_to: 'none'
version: 0.0.1
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.2.1
  flutter_map: ^6.2.1
  latlong2: ^0.9.1

flutter:
  uses-material-design: true
